name: hello2
on:
  workflow_call:
    inputs:
      runs-on:
        type: string
        default: "ubuntu-latest"
jobs:
  hello2:
    runs-on: ${{ inputs.runs-on }}
    steps:
      - run: echo "runs-on expression works"
