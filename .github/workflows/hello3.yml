name: hello3
on:
  workflow_call:
    inputs:
      working-directory:
        type: string
        default: "."
jobs:
  hello3:
    runs-on: ubuntu-latest
    steps:
      - run: echo "working-directory expression works"
        working-directory: ${{ inputs.working-directory }}
